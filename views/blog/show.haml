%article
    %h1= @post.title
    = haml :flashes
    ~ textile @post.body
    %p= print_tags(@post, :narrow => true)
    - if env['warden'].authenticated?
        %p= link_to 'Edit', @post.edit_path

